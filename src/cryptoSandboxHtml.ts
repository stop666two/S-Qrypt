export const cryptoSandboxHtml = `<!DOCTYPE html><html><head><meta charset="UTF-8"></head><body><script src="https://cdn.jsdelivr.net/npm/argon2-browser@1.18.0/dist/argon2.js" integrity="sha384-am7P6S/HiklM8ZO8Ihrv1tCPTFPGfEnKfUn8cqws88YeMDJAx8PJ3gCTdcS1XBuz" crossorigin="anonymous"></script><script>
const ctx='S-Qrypt-v2:PreMix',ctxB=new TextEncoder().encode(ctx),saltB=new TextEncoder().encode('S-Qrypt-Final');
function hex(b){return Array.from(new Uint8Array(b)).map(x=>x.toString(16).padStart(2,'0')).join('')}
function b64(b){let s='';for(let i=0;i<b.length;i++)s+=String.fromCharCode(b[i]);return btoa(s)}
function fromB64(s){const b=atob(s),r=new Uint8Array(b.length);for(let i=0;i<b.length;i++)r[i]=b.charCodeAt(i);return r}
function concat(a,b){const r=new Uint8Array(a.length+(b?b.length:0));r.set(a);if(b)r.set(b,a.length);return r}
function htb(h){const r=new Uint8Array(h.length/2);for(let i=0;i<h.length;i+=2)r[i/2]=parseInt(h.substr(i,2),16);return r}
const K256=new Uint32Array([0x428a2f98,0x71374491,0xb5c0fbcf,0xe9b5dba5,0x3956c25b,0x59f111f1,0x923f82a4,0xab1c5ed5,0xd807aa98,0x12835b01,0x243185be,0x550c7dc3,0x72be5d74,0x80deb1fe,0x9bdc06a7,0xc19bf174,0xe49b69c1,0xefbe4786,0x0fc19dc6,0x240ca1cc,0x2de92c6f,0x4a7484aa,0x5cb0a9dc,0x76f988da,0x983e5152,0xa831c66d,0xb00327c8,0xbf597fc7,0xc6e00bf3,0xd5a79147,0x06ca6351,0x14292967,0x27b70a85,0x2e1b2138,0x4d2c6dfc,0x53380d13,0x650a7354,0x766a0abb,0x81c2c92e,0x92722c85,0xa2bfe8a1,0xa81a664b,0xc24b8b70,0xc76c51a3,0xd192e819,0xd6990624,0xf40e3585,0x106aa070,0x19a4c116,0x1e376c08,0x2748774c,0x34b0bcb5,0x391c0cb3,0x4ed8aa4a,0x5b9cca4f,0x682e6ff3,0x748f82ee,0x78a5636f,0x84c87814,0x8cc70208,0x90befffa,0xa4506ceb,0xbef9a3f7,0xc67178f2]);
function rr(x,n){return(x>>>n)|(x<<(32-n))}
function sha256Pad(d){const ml=d.length*8,b=Math.ceil((d.length+9)/64),buf=new Uint8Array(b*64);buf.set(d);buf[d.length]=0x80;const dv=new DataView(buf.buffer);dv.setUint32(buf.length-8,ml>>>32,0);dv.setUint32(buf.length-4,ml&0xffffffff,0);return buf}
function sha256C(h,p){const w=new Uint32Array(64);for(let bl=0;bl<p.length;bl+=64){const dp=new DataView(p.buffer,bl,64);for(let t=0;t<16;t++)w[t]=dp.getUint32(t*4,0);for(let t=16;t<64;t++){const s0=rr(w[t-15],7)^rr(w[t-15],18)^(w[t-15]>>>3),s1=rr(w[t-2],17)^rr(w[t-2],19)^(w[t-2]>>>10);w[t]=(w[t-16]+s0+w[t-7]+s1)|0}let a=h[0],b_=h[1],c=h[2],d=h[3],e=h[4],f=h[5],g=h[6],hh=h[7];for(let t=0;t<64;t++){const S1=rr(e,6)^rr(e,11)^rr(e,25),ch=(e&f)^((~e)&g),t1=(hh+S1+ch+K256[t]+w[t])|0,S0=rr(a,2)^rr(a,13)^rr(a,22),maj=(a&b_)^(a&c)^(b_&c),t2=(S0+maj)|0;hh=g;g=f;f=e;e=(d+t1)|0;d=c;c=b_;b_=a;a=(t1+t2)|0}h[0]=(h[0]+a)|0;h[1]=(h[1]+b_)|0;h[2]=(h[2]+c)|0;h[3]=(h[3]+d)|0;h[4]=(h[4]+e)|0;h[5]=(h[5]+f)|0;h[6]=(h[6]+g)|0;h[7]=(h[7]+hh)|0}return h}
function sha256(m){if(typeof m==='string')m=new TextEncoder().encode(m);const h=new Uint32Array([0x6a09e667,0xbb67ae85,0x3c6ef372,0xa54ff53a,0x510e527f,0x9b05688c,0x1f83d9ab,0x5be0cd19]);const r=sha256C(h,sha256Pad(m));const o=new Uint8Array(32),dv=new DataView(o.buffer);for(let i=0;i<8;i++)dv.setUint32(i*4,r[i],0);return o}
async function hmac(k,d){const key=await crypto.subtle.importKey('raw',k,{name:'HMAC',hash:'SHA-512'},false,['sign']);return new Uint8Array(await crypto.subtle.sign('HMAC',key,d))}
async function sha512(m){if(typeof m==='string')m=new TextEncoder().encode(m);return new Uint8Array(await crypto.subtle.digest('SHA-512',m))}
async function hkdfE(prk,info,len){const ib=typeof info==='string'?new TextEncoder().encode(info):info;let r=new Uint8Array(0),t=new Uint8Array(0),c=1;while(r.length<len){const d=new Uint8Array(t.length+ib.length+1);d.set(t);d.set(ib,t.length);d[d.length-1]=c;t=await hmac(prk,d);const tmp=new Uint8Array(r.length+t.length);tmp.set(r);tmp.set(t,r.length);r=tmp;c++}return r.slice(0,len)}
async function deriveKA(mk,nid){const seed=concat(mk,concat(new TextEncoder().encode(String(nid)),new TextEncoder().encode('meta')));let st=seed;for(let i=1;i<=128;i++){const s512=await sha512(st),x=new Uint8Array(64);for(let j=0;j<64;j++)x[j]=s512[j];const rd=new Uint8Array(9);new DataView(rd.buffer).setUint32(0,i,0);new DataView(rd.buffer).setUint32(4,nid,0);rd[8]=0;st=await hmac(x,rd)}return await hkdfE(st,'KA',256)}
async function deriveKB(mk,nid){const tk=await hmac(mk,concat(new TextEncoder().encode(String(nid)),new TextEncoder().encode('body')));const ak=await crypto.subtle.importKey('raw',tk.subarray(0,32),{name:'AES-CBC'},false,['encrypt']);const iv=new Uint8Array(16),tb=new Uint8Array(2048);let prev=new Uint8Array(16);for(let i=0;i<64;i++){const enc=await crypto.subtle.encrypt({name:'AES-CBC',iv},ak,prev),ct=new Uint8Array(enc);tb.set(ct,i*32);prev=ct.subarray(0,16)}const fd=new Uint8Array(256);for(let g=0;g<8;g++){const off=g*256;for(let j=0;j<256;j++)fd[j]^=tb[off+j]}return fd}
async function deriveKC(mk,nid){const lvs=[];for(let i=0;i<8;i++){const inp=concat(mk,concat(new TextEncoder().encode(String(nid)),new Uint8Array([i])));lvs.push(sha256(inp))}const nds=[];for(let i=0;i<4;i++){const c=concat(lvs[i*2],lvs[i*2+1]);nds.push(sha256(c))}const ri=concat(nds[0],concat(nds[1],concat(nds[2],nds[3]))),rt=await sha512(ri);return await hkdfE(rt,'KC',256)}
async function encGCM(k,p){const ak=await crypto.subtle.importKey('raw',k.subarray(0,32),{name:'AES-GCM'},false,['encrypt']),iv=crypto.getRandomValues(new Uint8Array(12)),enc=await crypto.subtle.encrypt({name:'AES-GCM',iv},ak,p),r=new Uint8Array(12+enc.byteLength);r.set(iv);r.set(new Uint8Array(enc),12);return b64(r)}
async function decGCM(k,p){const d=fromB64(p);if(d.length<28)throw new Error('invalid');const iv=d.subarray(0,12),ct=d.subarray(12),ak=await crypto.subtle.importKey('raw',k.subarray(0,32),{name:'AES-GCM'},false,['decrypt']);return new Uint8Array(await crypto.subtle.decrypt({name:'AES-GCM',iv},ak,ct))}
onmessage=async function(e){const msg=e.data;try{let result;switch(msg.cmd){
case'deriveKA':{const mk=htb(msg.args[0]),nid=msg.args[1];result={key:hex(await deriveKA(mk,nid))};break}
case'deriveKB':{const mk=htb(msg.args[0]),nid=msg.args[1];result={key:hex(await deriveKB(mk,nid))};break}
case'deriveKC':{const mk=htb(msg.args[0]),nid=msg.args[1];result={key:hex(await deriveKC(mk,nid))};break}
case'encrypt':{const k=htb(msg.args[0]),pt=htb(msg.args[1]);result={data:await encGCM(k,pt)};break}
case'decrypt':{const k=htb(msg.args[0]),pkt=msg.args[1];result={data:hex(await decGCM(k,pkt))};break}
case'sha224':{const d=typeof msg.args[0]==='string'?new TextEncoder().encode(msg.args[0]):htb(msg.args[0]);result={hash:hex(sha256(d).slice(0,28))};break}
case'opToken':{const mk=htb(msg.args[0]),tok=await hkdfE(mk,'s-qrypt-op-auth',32);result={token:hex(tok)};break}
case'encryptNote':{const mk=htb(msg.args[0]),nid=msg.args[1],metaS=msg.args[2],bodyS=msg.args[3];const KA=await deriveKA(mk,nid),KB=await deriveKB(mk,nid),KC=await deriveKC(mk,nid);result={meta_packet:await encGCM(KA,new TextEncoder().encode(metaS)),body_packet:await encGCM(KB,new TextEncoder().encode(bodyS)),kc_hex:hex(KC)};break}
case'decryptNote':{const mk=htb(msg.args[0]),nid=msg.args[1],mPkt=msg.args[2],bPkt=msg.args[3];const KA=await deriveKA(mk,nid),KB=await deriveKB(mk,nid);result={meta:new TextDecoder().decode(await decGCM(KA,mPkt)),body:new TextDecoder().decode(await decGCM(KB,bPkt))};break}
case'decryptNoteMeta':{const mk=htb(msg.args[0]),nid=msg.args[1],mPkt=msg.args[2];result={meta:new TextDecoder().decode(await decGCM(await deriveKA(mk,nid),mPkt))};break}
case'argon2id':{try{if(typeof argon2==='undefined'||!argon2.hash){result={ok:false,error:'argon2_not_loaded'};break}const pw=msg.args[0],salt=msg.args[1]||'S-Qrypt-v2:PreMix';const h=await argon2.hash({pass:pw,salt:salt,time:3,mem:32768,par:4,type:argon2.ArgonType.Argon2id,hashLen:64});result={ok:true,hash:hex(new Uint8Array(h.hash))}}catch(e){result={ok:false,error:e.message}};break}
default:throw new Error('unknown cmd: '+msg.cmd);
}postMessage({id:msg.id,result})}catch(err){postMessage({id:msg.id,error:err.message})}};
</script></body></html>`;
