{
  "name": "s-qrypt",
  "version": "1.0.1",
  "private": true,
  "scripts": {
    "build": "tsc --noEmit",
    "deploy": "node scripts/check-clean.mjs && tsc --noEmit && node scripts/obfuscate.mjs && wrangler deploy && git checkout -- src/homeHtml.ts src/cryptoSandboxHtml.ts",
    "deploy:quick": "tsc --noEmit && wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "setup": "node scripts/setup.mjs",
    "test": "vitest",
    "cf-typegen": "wrangler types",
    "pretest": "node scripts/crypto-kat.mjs"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.12.4",
    "@types/node": "^26.1.1",
    "typescript": "^5.9.3",
    "vitest": "~3.2.0",
    "wrangler": "^4.82.2",
    "javascript-obfuscator": "^5.4.6"
  }
}
