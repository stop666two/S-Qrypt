# S-Qrypt 全面代码审查报告

> 审查范围：`src/index.ts`（服务端 Worker）、`src/homeHtml.ts`（客户端全部逻辑嵌入模板字符串）、`migrations/0001_init.sql`、`test/index.spec.ts`、`worker-configuration.d.ts`
> 
> 对照文档：`文档.txt`（436 行）
> 
> 审查日期：2026-07-10

---

## 目录

- [A. 架构性缺口 — 文档承诺但代码缺失](#a-架构性缺口--文档承诺但代码缺失)
- [B. 安全问题](#b-安全问题)
- [C. 性能问题](#c-性能问题)
- [D. 代码质量问题](#d-代码质量问题)
- [E. 数据连贯性问题](#e-数据连贯性问题)
- [F. 逻辑问题](#f-逻辑问题)
- [G. 界面/UX 问题](#g-界面ux-问题)
- [H. 测试问题](#h-测试问题)
- [I. TypeScript / 工程配置问题](#i-typescript--工程配置问题)

---

## A. 架构性缺口 — 文档承诺但代码缺失

### A1. Argon2id 混合增强（文档 §3.2.3）— 完全未实现

**文档描述**：
> 若浏览器支持高效 Argon2id（WebAssembly 检测通过），并行执行 Argon2id(密码, 盐=ctx, 内存=32MB, 并行度=4, 迭代=3)，输出 64 字节 `IK_argon`。将 `IK` 与 `IK_argon` 进行 HMAC-SHA512 交叉混合。

**代码位置**：`src/homeHtml.ts:444-542`（`deriveMasterKey` 函数）

**具体问题**：
1. `getAdaptiveParams()` 函数（L397-405）定义了 `enableArgon` 变量，但当 `cores>=8 && mem>=8` 时设为 `true`
2. `deriveMasterKey` 函数在阶段 2 之后（L528-535）**完全没有对 `enableArgon` 做任何判断**，直接跳过 Argon2id 进入 HKDF 淬炼
3. `kdf_version` 在客户端硬编码为 2（L1085: `appState.kdfVersion=2`），表示已启用 Argon2id，但实际从未执行
4. 将来若引入 Argon2id 实现，`INIT` 接口存储的 `kdf_version=2` 与老客户端不兼容——因为老客户端虽然也写 v2，但实际没跑 Argon2id，导致密钥派生路径不同，MK 不一致

**修复建议**：要么实现 Argon2id（WASM 加载），要么将 `kdf_version` 改为 `1` 并与文档保持一致。

---

### A2. Web Worker 并行内存混淆（文档 §3.2.2）— 完全未实现

**文档描述**：
> 创建与 `navigator.hardwareConcurrency/2` 匹配的 Web Worker，均分内存块。128 轮交叉混淆，每 8 轮进行跨 Worker 混合：交换 1 KB 数据块并重新哈希状态。

**代码位置**：`src/homeHtml.ts:462-511`

**具体问题**：
1. 内存混淆完全在主线程串行执行（L470-511 的 `for(let round=0; round<ROUNDS; round++)` 循环）
2. `navigator.hardwareConcurrency` 仅在 `getAdaptiveParams()` 中用于决定内存/轮数，从未用于创建工作线程
3. 缺少 `Worker` 构造函数调用、`postMessage` 通信、跨线程数据交换等任何多线程相关代码
4. 128 轮大计算量在主线程阻塞 UI

**修复建议**：使用 `new Worker()` 创建密码学 Worker，用 `postMessage`/`onmessage` 分发内存块并收集结果。

---

### A3. 沙箱 iframe 密码学隔离（文档 §3.9）— 完全未实现

**文档描述**：
> 将核心密码学代码（密钥派生、加解密）放置于一个独立的 `iframe` 中，该 `iframe` 使用 `sandbox="allow-scripts"` 属性，且 `src` 为专门的服务端点。`iframe` 与主页面通过 `postMessage` 通信，仅传递必要的命令和结果，密钥和明文永不进入主页面 JavaScript 上下文。

**代码位置**：`src/homeHtml.ts` 全局

**具体问题**：
1. 所有密码学函数（`deriveMasterKey`、`deriveKA`/`KB`/`KC`、`encryptGCM`/`decryptGCM`）直接定义在主页面 `<script>` 中（L217-L1308）
2. `appState.mk`、`appState.opToken`、`appState._keyCache` 等所有密钥存在于主页面全局对象上（L702-714）
3. 无 `postMessage` 通信机制
4. 无 `sandbox iframe` 创建代码

**影响**：任何 XSS 漏洞或恶意浏览器扩展可直接读取全部密钥材料。文档的安全模型完全依赖于这一隔离机制。

---

### A4. WebAssembly 密钥隔离（文档 §3.10）— 完全未实现

**文档描述**：
> 所有密码学原语（AES-256-GCM、SHA-512、BLAKE2b、HKDF 及自定义派生）编译为单个 WebAssembly 模块。主密钥 MK 和所有派生工作密钥仅存在于 Wasm 线性内存中，JavaScript 无法直接访问。Wasm 模块暴露的接口仅接受 ArrayBuffer。

**代码位置**：`src/homeHtml.ts` 全局

**具体问题**：
1. 全部密码学用纯 JavaScript 实现（L255-628）
2. 没有 `.wasm` 文件引用、`WebAssembly.instantiate()` 调用、或内存操作
3. MK 存放于 JS `Uint8Array` 中，JS 代码可直接读写（L702-714）
4. `clearMemory()` 的 `.fill(0)` 可能被 V8 优化跳过（但即使执行，也无法阻止已捕获的内存快照）

---

### A5. 透明日志存储与 Merkle 根提交（文档 §3.11）— 严重偏离

**文档描述**：
> 日志记录以加密形式存储于 IndexedDB，并定期将日志的 Merkle 树根哈希提交至一个公开的、不可篡改的存储（例如 Cloudflare Workers KV 端点）。

**代码位置**：`src/homeHtml.ts:643-667`

**具体问题**：
| 文档要求 | 代码实现 | 行号 |
|---------|---------|------|
| IndexedDB 加密存储 | `sessionStorage.setItem('sq_audit',JSON.stringify(stored))` | L665 |
| 定期 Merkle 根提交 | 无 | — |
| MK 派生日志加密密钥 | 仅签名（`audit-sign-key`），无加密 | L651 |
| 日志容量 500+ | 500 上限 | L664 |

**`sessionStorage` 的问题**：
- 会话级存储：关闭标签页丢失全部日志
- 明文存储：无加密
- 任何同源脚本均可读取

**日志签名密钥长度不一致**：文档说 256 字节，代码用 64 字节（L651: `hkdfExpand(appState.mk,'audit-sign-key',64)`）。

---

### A6. KB 派生使用 AES-CTR 而非 AES-CBC（文档 §3.4.2）— 算法偏离

**文档描述**：
> 将 TK 的前 32 字节作为 AES-256 密钥，对一个 16 字节全零明文块进行 256 次链式加密（实际可使用 **CBC 模式**连续加密，每次密文作为下一块明文）。

**代码位置**：`src/homeHtml.ts:565-587`

```typescript
// 实际代码使用 AES-CTR（L568）：
const aesKey = await crypto.subtle.importKey('raw', aesKeyBytes, { name: 'AES-CTR' }, false, ['encrypt']);
// ...
const enc = await crypto.subtle.encrypt({ name: 'AES-CTR', counter: ctr, length: 64 }, aesKey, prev);
```

**问题**：
- 文档明确指定了 **CBC 模式链式加密**，但代码使用了 **CTR 模式**
- CTR 模式本质是密钥流生成器 + XOR，链式依赖弱于 CBC
- 两者产生的密钥字节完全不同，违背了"文档规定即安全契约"的原则

---

### A7. Trusted Types 未实现（文档 §3.9）

**文档描述**：
> Trusted Types：开启后，所有 DOM 操作必须通过 Trusted Types 对象，杜绝 DOM XSS 漏洞。

**代码位置**：`src/index.ts:21`

实际 CSP 头：
```
Content-Security-Policy: default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'; object-src 'none';
```

缺少 `require-trusted-types-for 'script'` 策略。

---

### A8. 自适应参数惩罚未实际生效（文档 §3.8）

**文档描述**：
> 连续密码错误达 5 次，自动将派生参数提升至最高档（如 64 MB 内存、200 轮），增加单次尝试时间。

**代码位置**：`src/homeHtml.ts:1064-1068`

```typescript
if(failCount >= FAIL_THRESHOLD){
  failBadge.textContent = '⚠ 连续' + failCount + '次失败 · 安全锁定已加强';
  failBadge.style.display = 'inline-block';
}
```

仅显示文本徽章，未执行任何参数升级。且 `failCount` 仅在内存中，页面刷新即重置。

---

### A9. 控制流平坦化未实现（文档 §3.7）

**文档描述**：
> 密钥派生循环中控制流平坦化，所有条件分支由数据驱动，不依赖直接比较指令，避免执行路径泄露密钥特征。

代码中所有循环、条件均为常规控制流（`if`、`for`、`while`），无任何平坦化处理。

---

## B. 安全问题

### B1. CSP 使用 `'unsafe-inline'` — 违背文档严格 CSP 策略

**文件**：`src/index.ts:21`

```typescript
'Content-Security-Policy': "default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'; object-src 'none';"
```

**问题**：
- 文档 §3.9 要求：`default-src 'self'; script-src 'self'; style-src 'self'; object-src 'none'; require-trusted-types-for 'script'`
- 代码使用了 `'unsafe-inline'`（因为全部代码内联在 HTML 中），这完全破坏了 CSP 的 XSS 防护能力
- 任何注入的内联 `<script>` 或事件处理器（如 `onclick`）均可执行
- 缺少 `require-trusted-types-for 'script'` 策略

---

### B2. 验证令牌 SHA-224(P) 无盐 — 彩虹表攻击可行

**文件**：
- `src/homeHtml.ts:1060` — `const pwHash = hex(sha224(pw))`
- 文档 §3.3 确认：`verification_token = SHA-224(P)`

**问题**：
- `SHA-224(P)` 不加盐，同一密码在所有用户/部署中产生相同哈希
- 攻击者获取 `verification_token` 后可用彩虹表预计算逆推密码
- 虽然派生 MK 需要 ≥2 秒/次，但验证令牌仅 SHA-224，可快速枚举（每秒百万级）
- 加盐的缺失是密码学常识性错误

---

### B3. MK 存储于全局 JS 变量 — 违背零信任

**文件**：`src/homeHtml.ts:702-714`

```typescript
let appState = {
  mk: null,        // ← 主密钥在 JS 堆上
  opToken: null,   // ← 操作令牌在 JS 堆上
  // ...
  _keyCache: {},   // ← 全部派生密钥在 JS 堆上
};
```

**问题**：
- 文档 §3.10 要求 MK 仅存在于 Wasm 线性内存（JS 不可直接访问）
- 当前所有密钥材料完全暴露给任何同源运行的 JS
- XSS 或浏览器扩展可直接：`const mk = appState.mk`
- `clearMemory()` 的 `.fill(0)` 不可靠：V8 可能优化掉赋值，且 `.fill(0)` 的是新创建的副本而非原始 `ArrayBuffer`（L717）

---

### B4. 服务端无认证速率限制

**文件**：`src/index.ts:123-129`（`GET /api/token`）

**问题**：
- `verification_token` 可通过 `GET /api/token` 无限次获取
- 服务端无 IP 频率限制、无失败计数、无 CAPTCHA
- 攻击者可离线暴力枚举 SHA-224（约每秒 10^8 次）
- 文档 §3.12 提到"建议 Cloudflare 速率限制"，但代码本身未实现任何限流

---

### B5. 审计日志明文存于 sessionStorage

**文件**：`src/homeHtml.ts:662-666`

```typescript
const stored = JSON.parse(sessionStorage.getItem('sq_audit') || '[]');
stored.push(entry);
sessionStorage.setItem('sq_audit', JSON.stringify(stored));
```

**问题**：
- sessionStorage 明文存储，无加密
- 任何同源脚本均可读取、篡改
- 违反文档"加密存储于 IndexedDB"的要求
- 会话结束日志全部丢失

---

### B6. 多处空 catch 块吞异常

**文件**：`src/homeHtml.ts`

| 行号 | 代码 | 风险 |
|------|------|------|
| 423 | `catch(e){}` | `randomDelay` 中 AES 加密失败静默忽略，可能掩盖安全错误 |
| 504 | `catch(e){}` | 内存混淆中 AES 空加密失败静默忽略 |
| 654 | `catch(e){}` | 审计日志签名失败静默忽略 |
| 666 | `catch(e){}` | 日志序列化失败静默忽略 |

---

### B7. `constantTimeEqual` 通过长度提前返回 — 信息泄露

**文件**：`src/index.ts:31-38`

```typescript
function constantTimeEqual(a: string, b: string): boolean {
  if (a.length !== b.length) return false;  // ← 长度泄露！
  // ...
}
```

**问题**：攻击者可通过测量响应时间判断令牌长度。虽然长度固定（SHA-224=56 hex 字符，SHA-256=64 hex 字符），但此函数被 `requireOperationToken` 和 `requireVerificationToken` 调用，若将来使用变长输入则有风险。

---

### B8. 客户端 `hexSha256` 与服务器实现不一致

**文件**：
- 客户端：`src/homeHtml.ts:40-45`（`hexSha256` 使用 `crypto.subtle.digest('SHA-256')`）
- 服务器：`src/index.ts:40-45`（同名 `hexSha256` 也使用 `crypto.subtle.digest('SHA-256')`）

**实际风险小**，但客户端还自定义了 `sha256()` 同步函数（L303），而 `hexSha256` 用的是 Web Crypto，两者可能因编码差异产生不同结果：
- `sha256('abc')` 使用 `new TextEncoder().encode('abc')` ✓
- `hexSha256('abc')` 也使用 `TextEncoder` ✓
- 但若传入 `Uint8Array`，`sha256` 直接使用，`hexSha256` 先用 `TextEncoder` 编码 → **对二进制输入，两者结果不同**

---

## C. 性能问题

### C1. `ensureSchema` 每个请求都执行

**文件**：`src/index.ts:104`

```typescript
await ensureSchema(env.DB);
```

**问题**：每次 HTTP 请求都执行两个 `CREATE TABLE IF NOT EXISTS` SQL 语句。D1 有按请求计费模式，这浪费了数据库写入操作和延迟。对于高并发场景，可能引发竞态。

**建议**：移到 `scheduled` 事件或通过 wrangler 迁移一次性执行。事实上已有 `migrations/0001_init.sql` 但代码不用。

---

### C2. `refreshNoteList` 对所有笔记派生 KA + 解密

**文件**：`src/homeHtml.ts:1006-1017`

```typescript
for(const row of data.notes) {
  // 对每条笔记（包括不显示的）执行：
  const KA = await deriveKA(appState.mk, row.id);
  const metaBytes = await decryptGCM(KA, row.encrypted_meta_packet);
  // ...
}
```

**问题**：
- `deriveKA` 执行 128 轮 SHA-512 + BLAKE2b + HMAC（每次约 50-200ms）
- `decryptGCM` 调用 Web Crypto 的 AES-256-GCM 解密
- 若有 200 条笔记，登录时需要 200 × 150ms ≈ 30 秒的串行解密
- 仅显示 20 条但却处理全部笔记
- 无虚拟滚动优化

---

### C3. 服务端返回全部笔记无分页

**文件**：`src/index.ts:178-180`

```typescript
const rows = await env.DB.prepare(
  'SELECT id, encrypted_meta_packet, is_test, deleted FROM notes ORDER BY id ASC'
).all();
```

**问题**：
- 返回所有行，无 `LIMIT`/`OFFSET`
- 百万级笔记时 DB 查询 + 网络传输极慢
- 客户端已实现客户端分页（`displayedCount`），但服务端不支持

---

### C4. `randomDelay` 忙等待

**文件**：`src/homeHtml.ts:414-425`

```typescript
// 伪代码
for(let i=0; i<iters; i++){
  await crypto.subtle.encrypt(...);  // 有用工作
}
while(Date.now()-start < ms) {
  await new Promise(r => setTimeout(r, 1));  // ← 忙轮询
}
```

**问题**：最后一个 `while` 循环每 1ms 检查一次时间，CPU 空转。应采用：
```typescript
await new Promise(r => setTimeout(r, ms));
```

---

### C5. BLAKE2b 纯 BigInt 实现

**文件**：`src/homeHtml.ts:347-393`

**问题**：
- 使用 `BigInt`（`0x6a09e667f3bcc908n`）实现 64 位算术，BigInt 运算比 TypedArray + 手动 32-bit 拆分慢 10-50 倍
- 128 轮回旋哈希链（L549-559）中每轮调用一次 `blake2b(state, 64)`，开销巨大
- `b2Compress`（L357-369）每轮 12 次循环 × 8 次 `b2G` 调用

---

### C6. `fillMemoryAesCtr` 每次 1MB 块加密

**文件**：`src/homeHtml.ts:431-441`

```typescript
const CHUNK = 1048576;
for(let off=0; off<memory.length; off+=CHUNK) {
  const enc = await crypto.subtle.encrypt({name:'AES-CTR', ...}, key, new Uint8Array(len));
  memory.set(new Uint8Array(enc), off);
}
```

**问题**：
- 32MB 内存填充需 32 次异步加密往返
- 每次加密完执行 `memory.set`，32MB 数据拷贝
- 建议使用更大的块（如 4MB-8MB）或 Web Crypto 的 `encrypt` 流式处理

---

### C7. 密钥缓存仅 50 条

**文件**：`src/homeHtml.ts:823-833`

```typescript
const KEY_CACHE_MAX = 50;
while(KEY_CACHE_ORDER.length > KEY_CACHE_MAX){
  const evicted = KEY_CACHE_ORDER.shift();
  delete appState._keyCache['k_' + evicted];
}
```

**问题**：
- 用户浏览超过 50 条笔记时，之前的密钥被逐出，回看需重新派生（3 条 2048-bit 密钥 ≈ 300-600ms）
- 缓存空间以 256×3×50 ≈ 38KB 计，可安全扩大至 500-1000

---

## D. 代码质量问题

### D1. 1311 行模板字符串 — 无可维护性

**文件**：`src/homeHtml.ts:1-1311`

```typescript
export const homeHtml = `<!DOCTYPE html>
<html>
...
1311行全部内容...
</html>`;
```

**具体问题**：
1. **无语法高亮**：IDE 视整个字符串为普通文本
2. **无 ESLint**：所有的 JS 在字符串中，lint 工具无法分析
3. **无 TypeScript 检查**：即使文件是 `.ts`，模板内部的 JS 不受类型检查
4. **无模块化**：所有函数、变量、样式全在全局作用域
5. **调试困难**：浏览器调试时 source map 对应到字符串位置而非源文件
6. **协作困难**：CSS/HTML/JS 修改都在同一字符串中，diff 难以阅读

**建议**：拆分 HTML 为独立文件，使用构建工具（如 Vite/Rollup）处理，或至少将 JS 提取到单独文件后通过 `import` 引入。

---

### D2. 自定义 SHA-256 实现与 Web Crypto SHA-512 混用

**文件**：
- `src/homeHtml.ts:265-318` — 自定义同步 `sha256`/`sha224`
- `src/homeHtml.ts:321-324` — 基于 Web Crypto 的异步 `sha512`
- `src/homeHtml.ts:326-330` — 基于 Web Crypto 的异步 `hmac`

**问题**：
| 函数 | 同步/异步 | 底层实现 | 调用方一致性 |
|------|----------|---------|------------|
| `sha256(msg)` | ✅ 同步 | 纯 JS | 可直接赋值 |
| `sha224(msg)` | ✅ 同步 | 纯 JS | 可直接赋值 |
| `blake2b(input)` | ✅ 同步 | 纯 JS BigInt | 可直接赋值 |
| `sha512(msg)` | ❌ 异步 | `crypto.subtle.digest` | 必须 `await` |
| `hmac(key,data)` | ❌ 异步 | `crypto.subtle.sign` | 必须 `await` |

**后果**：
- 开发者需要牢记每个函数的同步/异步属性
- `deriveKA` 中 SHA-512 需要 `await` 但 BLAKE2b 不需要，混合使用易错
- 自定义 SHA-256 实现增加了攻击面和维护成本（且可能不如原生实现安全）

---

### D3. 多处空 catch 块

**文件**：`src/homeHtml.ts`

| 行号 | 代码原文 | 问题 |
|------|---------|------|
| 423 | `}catch(e){}` | `randomDelay` 中 AES 空加密失败时完全忽略 |
| 504 | `}catch(e){}` | 内存混淆中 AES-CBC 加密失败时完全忽略 |
| 654 | `}catch(e){}` | 审计日志签名失败时回退到 'unsigned'，但异常被吞 |
| 666 | `}catch(e){}` | sessionStorage 写失败时忽略（隐私模式下可能抛出） |

---

### D4. `clearMemory()` 的 `.fill(0)` 无效

**文件**：`src/homeHtml.ts:716-727`

```typescript
function clearMemory() {
  if (appState.mk) {
    const v = new Uint8Array(appState.mk);  // ← 创建了 appState.mk 的副本！
    v.fill(0);                                //  仅清除了副本，原 ArrayBuffer 仍在
  }
  appState.mk = null;
  // ...
}
```

**V8 层面的问题**：
1. `new Uint8Array(appState.mk)` 创建了一个 **新的 TypedArray 共享同一 `ArrayBuffer`**？不，`appState.mk` 本身就是 `Uint8Array`。当传入 `Uint8Array` 给 `Uint8Array` 构造时，**它会创建一个副本**。
2. 因此 `v.fill(0)` 清除的是副本，**原来的 `appState.mk` 的 `ArrayBuffer` 中的密钥数据仍然在内存中**
3. 正确的做法：`appState.mk.fill(0)` 直接在原对象上操作
4. 但即使这样做，V8 仍可能保留之前被 GC 掉的 `ArrayBuffer` 的零化副本

---

### D5. `notes.sort` 异常排序

**文件**：`src/homeHtml.ts:1019-1022`

```typescript
notes.sort((a,b) => {
  if(!a.created_at) return -1;   // ← damaged 笔记排最前
  if(!b.created_at) return 1;
  return a.created_at < b.created_at ? -1 : a.created_at > b.created_at ? 1 : 0;
});
```

**问题**：
- `created_at` 为空的损坏笔记排在最前（`return -1`）
- 用户首先看到不可读的损坏条目
- 更合理：排在最后

---

### D6. TypeScript 声明冲突

**文件**：`worker-configuration.d.ts:6-83`

```typescript
// 自定义重新声明了全局 Web API
declare class Response { ... }
declare class Request { ... }
declare class Headers { ... }
declare class URL { ... }
```

**问题**：
- Cloudflare Workers 运行时已有这些全局类
- 自定义声明与平台类型冲突，可能导致 `tsc` 编译错误或类型混淆
- 应使用 `@cloudflare/workers-types` 官方类型包

---

### D7. 自定义 Base64 编码在 Worker 上下文中存在风险

**文件**：`src/index.ts` 中未使用自定义 base64，但 `src/homeHtml.ts:236-246` 中有自定义 `b64`/`fromB64`

问题不大，但 `btoa`/`atob` 在 Workers 中受 `nodejs_compat` 兼容标志支持，没有额外安全性。

---

## E. 数据连贯性问题

### E1. 时间戳仅存于加密元数据

**文件**：
- 数据库 schema：`migrations/0001_init.sql` / `src/index.ts:51-52`
- 元数据构建：`src/homeHtml.ts:973`

**问题**：
- 文档中提到了创建/修改时间，但 `notes` 表没有 `created_at`/`updated_at` 列
- 时间戳嵌入在 `encrypted_meta_packet` 的 JSON 中
- 若 KA 派生逻辑变化或元数据损坏，**全部笔记的时间信息永久丢失**
- 无法在服务端按时间排序

---

### E2. 多标签页并发冲突

**文件**：`src/homeHtml.ts:961-1002` — `saveNote` 函数

**场景**：
1. Tab A 打开笔记 v1，开始编辑
2. Tab B 打开同一笔记，修改并保存（PUT → 密文更新）
3. Tab A 点击保存 → 用自己缓存的数据覆盖 Tab B 的更改
4. **无版本号、无 `If-Unmodified-Since`、无冲突检测**

---

### E3. 初始化流程非事务性

**文件**：`src/homeHtml.ts:1109-1210` — `handleInit` 函数

**断点序列**：
1. `POST /api/note` → 创建占位笔记成功 ✅
2. `PUT /api/note/:id` → 更新密文成功 ✅
3. `POST /api/init` → 写入配置失败 ❌

**结果**：遗留一个已加密但 config 表中无记录的孤立笔记，且数据库未标记 `init_completed=1`。下次初始化可能重复创建。

---

### E4. `kdf_version` 客户端/服务端不一致

**文件**：
- 服务端存储：`src/index.ts:167` — 客户端传入的 `kdf_version`
- 客户端硬编码：`src/homeHtml.ts:1085` — `appState.kdfVersion = 2`
- 客户端初始化传值：`src/homeHtml.ts:1178` — `kdf_version: 2`
- 文档中 kdf_version=1 表示基础强化，2 表示含 Argon2id

**问题**：
- 代码中 Argon2id **从未实现**，但 `kdf_version` 始终写 2
- 未来真实实现 Argon2id 后，现有已存储的 `kdf_version=2` 的数据与新行为不兼容
- 客户端登录时从不检查服务端的 `kdf_version`（L1085 始终设 2）

---

### E5. 损坏笔记数据不完整

**文件**：`src/homeHtml.ts:1014-1016`

```typescript
notes.push({
  id: row.id,
  title: '',       // 无标题
  created_at: '',  // 无时间
  updated_at: '',  // 无时间
  damaged: true,
});
```

**问题**：解密失败的笔记不保留任何元数据。但 `encrypted_meta_packet` 仍然存在，理论上如果能修复派生逻辑或密钥，可以重新解密。当前设计丢弃了未来恢复的可能性。

---

## F. 逻辑问题

### F1. 🔴 删除逻辑反直觉 — 最危险的 UX 错误

**文件**：`src/homeHtml.ts:940-958`

```typescript
async function deleteNote(id) {
  const hard = await showConfirm('软删除（可恢复）? 取消=硬删除');
  //    ↑ 点"确认" → hard = true → 执行软删除
  //      点"取消" → hard = false → 执行硬删除
  try {
    if (hard) {
      // hard=true 时执行软删除！
      await apiFetch('/api/note/'+id+'/soft-delete', ...);
    } else {
      // hard=false 时执行硬删除！
      await apiFetch('/api/note/'+id, {method: 'DELETE', ...});
    }
  }
}
```

**具体问题**：
| 用户操作 | 用户预期 | 实际行为 | 后果 |
|---------|---------|---------|------|
| 点"确认" | 删除笔记 | 软删除（可恢复） | 符合预期 ✅ |
| 点"取消" | 取消操作，什么都不做 | **永久硬删除！** | 数据不可逆丢失 ❌❌ |

**变量命名混淆**：变量名 `hard` 含义反转——`hard=true` 时执行软删除，`hard=false` 时执行硬删除。

---

### F2. 第二次确认对话框在"取消=硬删除"之后才弹出

**文件**：`src/homeHtml.ts:949-952`

```typescript
if (hard) { /* 软删除 */ }
else {
  const sure = await showConfirm('确认永久删除？不可恢复！');
  if(!sure) return;
  await apiFetch('/api/note/'+id, {method: 'DELETE', ...});
}
```

当用户点击"取消"后（以为取消了操作），才弹出"确认永久删除"对话框——即使用户在这里点"取消"，已经比预期多了一步危险操作。

---

### F3. 笔记正文内容样式 class 被覆盖

**文件**：`src/homeHtml.ts:913-914`

```typescript
document.getElementById('detail-body').textContent = bodyText || '(空内容)';
document.getElementById('detail-body').className = 'detail-body' + (bodyText?'':' detail-body-empty');
```

**问题**：`className` 直接赋值覆盖了原始 CSS 类（包括所有样式）。仔细检查 HTML（L166）发现 `detail-body` 只有 `class="detail-body"`，此处似乎仍是完整赋值。但若未来增加其他类（如动画类、语言类），会被覆盖删除。

安全起见应使用 `classList.add`/`remove`。

---

### F4. 记忆体回退时使用不一致的长度

**文件**：`src/homeHtml.ts:464-465`

```typescript
let memory;
let actualSize = MEM_SIZE;
try { memory = new Uint8Array(MEM_SIZE); }
catch(e) { actualSize = 16*1024*1024; memory = new Uint8Array(actualSize); }
```

**问题**：
- `MEM_SIZE` 来自结构解构（L463），是 `const`
- `actualSize` 在 catch 块中更新为 16MB
- 但后续代码：`for(let round=0; round<ROUNDS; round++)` 中使用的偏移计算：
  ```typescript
  const off1 = new DataView(h1.buffer).getUint32(0,true) % actualSize;
  ```
  这里用了 `actualSize` ✅
- 但是 `fillMemoryAesCtr(memory, H)` 用的是原始的 `MEM_SIZE` 还是 `actualSize`？L468 传入 `memory`，但 `memory` 已在 catch 中为 16MB ✅
- 整体 OK，但变量命名混淆

---

### F5. `sha256Pad` 对大输入处理不当

**文件**：`src/homeHtml.ts:266-276`

```typescript
function sha256Pad(data) {
  const ml = data.length * 8;
  const blocks = Math.ceil((data.length + 9) / 64);
  const buf = new Uint8Array(blocks * 64);
  buf.set(data);
  buf[data.length] = 0x80;
  const dv = new DataView(buf.buffer);
  dv.setUint32(buf.length - 8, ml >>> 32, false);
  dv.setUint32(buf.length - 4, ml & 0xffffffff, false);
  return buf;
}
```

**问题**：
- `data.length` 是字节数，`ml = data.length * 8` 是位数
- 当 `data.length` 为 0 时，`blocks = Math.ceil(9/64) = 1`，`ml = 0`
- 当 `data.length` 恰好为 55 时，`blocks = Math.ceil(64/64) = 1`，但实际标准需要 2 块（1 byte padding + 8 bytes length = 9，55+9=64）
- 不对，`data.length=55` 时 `(55+9)/64 = 64/64 = 1.0`，`ceil(1.0)=1`，但 55+1+8=64，恰好 1 块 ✅
- `data.length=56` 时 `(56+9)/64 = 65/64 = 1.015625`，`ceil=2` ✅
- 公式正确

---

### F6. 初始化时 `verificationToken` 过早设置

**文件**：`src/homeHtml.ts:1140`

```typescript
// Set state early so API calls can use auth
appState.verificationToken = pwHash;
```

此时只完成了派生 MK 和 operation token，**尚未调用 `POST /api/init`**。如果后续 API 调用失败（L1143-1169 或 L1173-1180），`appState.verificationToken` 已被设置为一个未经服务端确认的值。后续 `apiFetch` 会带上这个 token（L773-775），可能导致操作使用未经验证的 token。

---

## G. 界面/UX 问题

### G1. 删除操作对话流程危险

**详见 F1/F2**。最严重的 UX 问题。建议：
1. 第一次确认："确定要删除此笔记吗？"
2. 选择操作："软删除（可恢复）" / "永久删除" / "取消"
3. "永久删除"需二次确认

当前逻辑可能导致用户以为是"取消操作"却实际触发了永久删除。

---

### G2. 初始加载时屏幕闪烁

**文件**：`src/homeHtml.ts:1256`

```typescript
document.addEventListener('DOMContentLoaded', () => {
  checkInit();  // 异步检查
```

**问题**：
- 页面加载时所有 `.screen` 元素初始可见（L13 `.screen` 的 `display: flex`）
- `checkInit` 是异步函数，在返回前所有 screen 同时可见
- `checkInit` 之后根据状态隐藏/显示对应 screen
- 在慢网络下用户会看到闪烁的 UI 切换

---

### G3. 密码字段 `autocomplete="off"` 已失效

**文件**：`src/homeHtml.ts:123,136-137`

```html
<input type="password" id="login-password" placeholder="输入主密码" autocomplete="off">
```

**问题**：现代浏览器（Chrome 90+, Firefox 90+）忽略 `autocomplete="off"` 的密码字段，始终提示保存密码。应使用 `autocomplete="new-password"`（表示新密码创建）来避免自动填充。

---

### G4. `user-scalable=no` 违反无障碍规范

**文件**：`src/homeHtml.ts:5`

```html
<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
```

**问题**：
- `user-scalable=no` 和 `maximum-scale=1.0` 禁止用户缩放页面
- 违反 WCAG 2.0 成功标准 1.4.4（Resize text）
- 视障用户无法放大页面阅读
- iOS Safari 上可能导致可访问性问题

---

### G5. 编辑时返回不提示保存

**文件**：`src/homeHtml.ts:1291-1294`

```typescript
document.getElementById('detail-back').addEventListener('click', () => {
  document.getElementById('detail-view').classList.remove('active');
  appState.currentNoteId = null;
});
```

如果用户从详情页进入编辑模式（L1296-1301），编辑器中修改了内容但没有保存，此时点击"← 返回"键：
- 编辑器关闭（没问题，编辑器自身有关闭按钮）
- 但详情页的返回按钮不检查 `editor-overlay` 是否 active

实际上编辑器的"返回"行为在 L1303-1304 处理，详情页的返回键不会关闭编辑器。但代码逻辑中，从详情点编辑 → 编辑器打开 → 此时点详情页返回（需要先手动关闭编辑器）→ 情况复杂。

**建议**：在编辑器中添加"未保存更改"的离开确认。

---

### G6. Toast 停留时间不足

**文件**：`src/homeHtml.ts:788-789`

```typescript
el._toastTimer = setTimeout(() => el.classList.remove('show'), 3000);
```

**问题**：
- 错误消息（尤其是解密失败、删除失败）通常包含较多信息
- 3 秒自动消失，错误可能来不及阅读
- 建议：成功消息 2-3s，错误消息保持直到用户交互

---

### G7. 无加载/进度指示器

- 新建笔记保存时按钮显示"保存中..." ✅（L966）
- 但 `refreshNoteList` 调用时无指示器
- `openNote` 解密时无旋转加载图标
- 列表页"加载更多"不显示加载状态

---

### G8. 损坏笔记不显示任何日期

**文件**：`src/homeHtml.ts:1014-1016`

损坏笔记的 `created_at` 和 `updated_at` 都为空字符串：
- `formatTime('')` → `Invalid Date`，因为 `new Date('')` 返回 `Invalid Date`
- `isNaN(Invalid Date - now)` 在 `formatTime` 的 L801-806 中行为未定义
- 实际上 `formatTime` 未检查 `d` 是否为有效日期

---

## H. 测试问题

### H1. 测试中硬编码 Token 哈希

**文件**：`test/index.spec.ts:5`

```typescript
const TEST_TOKEN_HASH = '6ca13d52ca70c883e0f0bb101e425a89e8624de51db2d2392593af6a841bf90d';
```

**问题**：
- 哈希值对应的原文是 `'abc123test'`，但测试中未注释说明
- 若未来 `hexSha256` 实现变化，此哈希需要同步更新
- 应通过程序化地调用 `hexSha256(TEST_TOKEN)` 生成

---

### H2. 缺少客户端密码学测试

**问题**：
- `test/index.spec.ts` 仅测试服务端 API
- 无任何测试覆盖客户端密码学函数：
  - `deriveMasterKey`（无法在 Node 环境运行，依赖 Web API）
  - `deriveKA`/`deriveKB`/`deriveKC`
  - `encryptGCM`/`decryptGCM` 加解密一致性
  - `sha256`/`sha224`/`blake2b` 向量测试
- 无已知答案测试（KAT）：无法验证密码学实现正确性

---

### H3. 测试可能使用了错误的 Worker 绑定

**文件**：`test/index.spec.ts:8-9`

```typescript
const res = await env.DB.fetch(`http://localhost${path}`, { ... });
```

`cloudflare:test` 中的 `env.DB` 是测试绑定，但测试中直接对其调用 `.fetch()` — 确认了这是通过 `SELF` 或 `env.DB` 的 `fetch` 入口调用 Worker。需要确认 `vitest-pool-workers` 配置是否正确。

---

## I. TypeScript / 工程配置问题

### I1. `tsconfig.json` 中 `target: es2024` 与 Workers 兼容性

**文件**：`tsconfig.json:3` — `"target": "es2024"`

**问题**：
- Cloudflare Workers 运行时的 V8 版本可能不完全支持 ES2024
- wrangler 在构建时会转译，但某些 ES2024 特性可能意外保留
- 建议与 `compatibility_date: "2026-07-10"` 对照验证

---

### I2. 无 `strict: true` 的完整严格检查

**文件**：`tsconfig.json:14` — `"strict": true`

虽然设置了 `strict: true`，但由于 JS 代码都在模板字符串中，不受 TypeScript 检查。这意味着 `strict` 只覆盖了 `src/index.ts` 和 `src/homeHtml.ts` 的导出/导入部分。

缺少的严格检查：
- `noUncheckedIndexedAccess`
- `noImplicitReturns`
- `exactOptionalPropertyTypes`

---

### I3. `wrangler.jsonc` 中 D1 数据库 ID 为空

**文件**：`wrangler.jsonc:11` — `"database_id": "local-dev-dummy-id"`

**问题**：
- 部署前必须替换为真实 D1 数据库 ID
- 但 `wrangler.jsonc` 中标注的 `database_name: "s-qrypt-db"` 可能与实际部署不一致
- 文档建议用 `wrangler d1 create s-qrypt-db` 创建，但创建的 ID 需要手动填入

---

### I4. `package.json` 缺少依赖

**文件**：`package.json:14-20`

**问题**：
- 无 `@cloudflare/workers-types` 依赖（但有 `worker-configuration.d.ts` 手动声明）
- 无前端构建依赖（Vite/Webpack/Rollup）——因为全部内联
- 无 `prettier`、`eslint`、`lint-staged` 等代码质量工具
- 无 `husky` pre-commit hooks

---

## 问题汇总

| 类别 | 🔴 严重 | 🟡 中 | 🟢 低 | 总计 |
|------|---------|-------|-------|------|
| A. 架构缺口 | 5 | 3 | 2 | 10 |
| B. 安全 | 6 | 2 | 0 | 8 |
| C. 性能 | 3 | 3 | 1 | 7 |
| D. 代码质量 | 1 | 5 | 2 | 8 |
| E. 数据连贯 | 1 | 3 | 1 | 5 |
| F. 逻辑 | 2 | 3 | 0 | 5 |
| G. UI/UX | 2 | 4 | 1 | 7 |
| H. 测试 | 0 | 3 | 0 | 3 |
| I. 工程配置 | 0 | 2 | 2 | 4 |
| **总计** | **20** | **28** | **9** | **57** |

### 最需要优先修复的 5 个问题

| 优先级 | 问题 | 类别 | 说明 |
|--------|------|------|------|
| P0 | **F1/F2 — 删除逻辑反转** | 逻辑/UX | 取消操作导致永久硬删除，可能造成不可逆数据丢失 |
| P0 | **A1 — Argon2id 未实现但 kdf_version=2** | 架构 | 密码学承诺与实现不匹配，影响未来兼容性 |
| P1 | **A3/A4 — 无沙箱/Wasm 隔离** | 安全 | 密钥完全暴露在 JS 全局作用域 |
| P1 | **B1 — CSP unsafe-inline** | 安全 | 违背文档承诺的 XSS 防护策略 |
| P1 | **B2 — SHA-224 无盐验证令牌** | 安全 | 彩虹表攻击可逆推密码 |
